**硬件配置**：前方双红外 + 双轮差速 + Hi3861/STM32

**功能实现**：巡线、Y字形岔路转弯、终点识别

**核心原理**

- 红外传感器发射红外光到地面，白色背景反射强、黑色胶布吸收红外光反射弱，接收管据此输出不同的电平信号，从而判断小车相对于黑线的位置。

| 左传感器 | 右传感器 | 含义             |
| ---- | ---- | -------------- |
| 白    | 白    | 小车居中，直行        |
| 黑    | 白    | 小车偏右，需左转修正     |
| 白    | 黑    | 小车偏左，需右转修正     |
| 黑    | 黑    | 特殊情况（岔路/终点/脱轨） |

- 白：未检测到黑线

- 黑：检测到黑线




**系统架构与分工**

Hi3861 和 STM32 各司其职：

- STM32（底层实时控制）：负责红外传感器 GPIO 读取、PWM 电机驱动、巡线状态机判断，要求响应快、确定性强
- Hi3861（上层决策与通信）：负责岔路策略决策（走哪条分支）、终点逻辑判断、WiFi 通信上报状态，通过串口与 STM32 交互

两者之间通过 UART 串口 通信，Hi3861 下发指令（如"下一个岔路左转"），STM32 上报传感器状态和事件（如"检测到岔路"）。

**一、基础巡线实现**

**二、岔路转弯实现**

- 双传感器方案识别岔路的核心思路是：利用"双黑"状态持续的时间长度来区分岔路口类型。

- 实际场地：两个Y型岔路口，并且正确道路均为左转的那条路线

**岔路类型判断**

| 场景        | 双黑持续时间   | 含义            |
| --------- | -------- | ------------- |
| 正常巡线中短暂双黑 | 极短（几毫秒）  | 黑线较宽或弯道过渡，忽略  |
| Y字路口      | 中等持续时间   | 斜向有黑线分支       |
| T字双黑线终点   | 两段持续时间较长 | 横向有黑线并且有黑白黑转变 |
| T字单黑线岔路口  | 一段持续时间较长 | 横向有黑线并且有黑白转变  |




- 具体的 CROSS_THRESHOLD 和 T_THRESHOLD 数值需要根据你的实际车速测试调整——先让小车跑一遍赛道，用串口打印出每次经过岔路时的计数值，再据此设定阈值。

**岔路转弯执行**

Hi3861 收到岔路事件后，根据预设路径策略（两个路口均左转）下发转向

转弯完成后的重新寻线：转弯后双传感器可能暂时都看不到黑线，此时应维持转弯动作直到某一侧重新检测到黑线，再切回正常巡线逻辑。

**三、终点识别实现**

**四、调试建议**

1. 先单独验证传感器：把小车架空，用手遮住/露出传感器，串口打印确认高低电平正确
2. 低速巡线先行：先把 BASE_SPEED 设得很低（如全速的 20%），确认巡线逻辑正确后再逐步提速
3. 记录岔路计数值：跑赛道时用串口打印 double_black_count，记录每个岔路和终点的实际计数值，据此精确设定阈值
4. 分段测试：先测直道巡线 → 再测弯道 → 再测岔路转弯 → 最后测终点识别，逐段验证

**Y型岔路的特征分析**

- Y型岔路是主路到达分叉点后，黑线分成两条斜向分支（夹角在 30°~90° 之间）

- 小车经过Y型分叉点时，双传感器的状态变化序列是：

阶段 左传感器 右传感器 说明
接近分叉点 白 白 正常直行
到达分叉点 黑 黑 两侧都压到分叉线
驶入某条分支 黑/白 白/黑 重新进入单侧检测



**阈值怎么确定**

1. 先让小车以目标速度跑赛道，串口打印每次双黑的 double_black_count 值
2. 你会看到三类数值区间：
   - 直道波动：1~3 个周期（极短）
   - Y型岔路：比如 5~15 个周期（中等）
   - 终点/特殊标记：20+ 个周期（很长）
3. 取中间值作为 Y_THRESHOLD，取较大值作为 CROSS_THRESHOLD

**Y型岔路转弯执行**

**转弯后重新寻线**

Y型岔路转弯后，小车进入分支时可能短暂丢失黑线。关键是在转弯过程中不要急切回巡线模式，而是等到传感器重新捕获黑线后再切换：

完整控制流程总结

主循环:
├── 读取传感器
├── 正常巡线（单侧检测 → 差速修正）
├── 检测到双黑 → 开始计时
│   └── 双黑结束 → 判断类型
│       ├── 短 → 忽略（线宽波动）
│       ├── 中 → Y型岔路 → 上报Hi3861
│       └── 长 → 终点/十字 → 上报Hi3861
├── 收到Hi3861转弯指令 → 执行Y型弧转 → 重新寻线
└── 收到终点指令 → 停车


